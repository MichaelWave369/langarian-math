# Usage

Run an example with `langarian run examples/basic_369.yaml --receipts-dir receipts`.

Check a receipt file with `langarian validate receipts/basic_369_bridge.json`.

Print a readable receipt summary with `langarian explain receipts/basic_369_bridge.json`.

These commands support the public Ledger workflow while keeping interpretive claims out of formal proof contexts.
